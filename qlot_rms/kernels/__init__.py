"""Experimental serving kernels for Q-LOT-DC (custom_packed backend).

These are CORRECTNESS-FIRST prototypes, not optimized kernels. No speedup is
claimed. ``triton_packed`` degrades gracefully when Triton is unavailable.
"""
